export interface Layout {
  i: string;
  x: number;
  y: number;
  w: number;
  h: number;
}

export interface WidgetType {
  id: string;
  type: string;
  title: string;
  x: number;
  y: number;
  w: number;
  h: number;
}
